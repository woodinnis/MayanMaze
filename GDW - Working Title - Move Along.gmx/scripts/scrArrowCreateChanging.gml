// Inherit create event from Arrow parent
event_inherited();

// Set boolean to check if player has touched the tile
clickMe = false;

direction = 0;

// Set image index and speed to 0, pointing at 0 degrees (right)
image_index = 0;
image_speed = 0;
